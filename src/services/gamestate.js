import { GameStateModel } from '../models/gamestate';

const createGameState = (roomId, availableRoles, players) => {
    const gameState = {
        roomId,
        availableRoles,
        players
    }
}