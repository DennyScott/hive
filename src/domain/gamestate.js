import { GameStateModel } from '../data/gamestate';

const createGameState = (roomId, availableRoles, players) => {
    const gameState = {
        roomId,
        availableRoles,
        players
    }
}